/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HorizontalControlDirective } from './horizontal-control.directive';

describe('Directive: HorizontalControl', () => {
  it('should create an instance', () => {
    const directive = new HorizontalControlDirective();
    expect(directive).toBeTruthy();
  });
});
